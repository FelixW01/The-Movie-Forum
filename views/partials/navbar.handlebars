<div class="navbar mb-1 shadow-lg bg-slate-900 text-neutral-content text-white text-lg">
  <div class="flex-1 px-2 mx-2">
    <span class="text-lg font-bold">
      <a href="/">Movie Forum</a>
    </span>
  </div>
  <a class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base" href="/dashboard">Dashboard</a>
  {{#if loggedIn}}
  <a class="" href="/dashboard/new">
  <button type="submit" class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base">+ Add New Post</button></a>
  
  <a class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base" href="/" id="logout">Logout</a>

  {{else}}
  <a class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base" href="/login">Login</a>
  <a class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base" href="/register">Sign Up</a>
  {{/if}}
</div>