<div class="navbar mb-1 shadow-lg bg-slate-900 text-neutral-content text-white text-lg">
  <div class="flex-1 px-2 mx-2">
    <span class="text-white text-lg font-bold">
      <a class="sm:text-4xl text-xl" style="font-family: 'Acme'" href="/">Movie Forum</a>
    </span>
  </div>

  <a class="btn text-base sm:text-2xl mx-auto btn-ghost btn-sm rounded-btn text-2xl font-bold text-white" style="font-family: 'Acme'" href="/dashboard">Dashboard</a>

  {{#if loggedIn}}

  <a class="btn text-base mx-auto btn-ghost btn-sm rounded-btn sm:text-2xl font-bold text-white" style="font-family: 'Acme'" type="submit" href="/dashboard/new">+ Add New Post</a>
  
  <a class="btn text-base mx-auto btn-ghost btn-sm rounded-btn text-2xl font-bold sm:text-2xl text-white" style="font-family: 'Acme'" href="/dashboard/new" href="/" id="logout">Logout</a>

  {{else}}
  <a class="btn text-base mx-auto btn-ghost btn-sm rounded-btn text-2xl font-bold sm:text-2xl text-white" style="font-family: 'Acme'" href="/login">Login</a>

  <a class="btn text-base mx-auto btn-ghost btn-sm rounded-btn text-2xl font-bold sm:text-2xl text-white" style="font-family: 'Acme'" href="/register">Sign Up</a>
  {{/if}}
</div>

