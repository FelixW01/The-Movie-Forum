<div class="navbar mb-1 shadow-lg bg-slate-900 text-neutral-content text-white text-lg">
  <div class="flex-1 px-2 mx-2">
    <span class="text-lg font-bold">
      <a class="text-4xl" style="font-family: 'Acme'" href="/">Movie Forum</a>
    </span>
  </div>

  <a class="btn btn-ghost btn-sm rounded-btn text-2xl font-bold text-white" style="font-family: 'Acme'" href="/dashboard">Dashboard</a>


  {{#if loggedIn}}
  {{!-- <a class="btn btn-ghost btn-sm rounded-btn text-2xl font-bold text-white" style="font-family: 'Acme'" href="/dashboard/new"></a> --}}

  <a class="btn btn-ghost btn-sm rounded-btn text-2xl font-bold text-white" style="font-family: 'Acme'" type="submit">+ Add New Post</a>
  
  <a class="btn btn-ghost btn-sm rounded-btn text-2xl font-bold text-white" style="font-family: 'Acme'" href="/dashboard/new" href="/" id="logout">Logout</a>

  {{else}}
  <a class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base" href="/login">Login</a>
  <a class="btn btn-ghost btn-sm rounded-btn text-xl font-bold text-white lg:text-base" href="/register">Sign Up</a>
  {{/if}}
</div>