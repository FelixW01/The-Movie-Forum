
<div class="forum-post card w-96 shadow-xl p-20 bg-red-50 hover:bg-indigo-500">
  <div>
    u/{{user.username}}
    <h3><a href="/post/{{id}}" class="post-header">{{title}}</a></h3>
  </div>

  <div>
    {{#if img}}
      <img class="block w-5/5 rounded-xl"
        src="https://image.tmdb.org/t/p/original/{{img}}"
        alt="img_content"
      />
    {{/if}}
    <p class="text-center text-lg font-semibold">{{content}}</p>
  </div>

</div>


  {{!-- <div class="flex gap-3 p-1 shadow-xl">
      {{#if img}}
        <img class="block w-24 rounded-xl"
          src="https://image.tmdb.org/t/p/original/{{img}}"
          alt="img_content"/>
      <div class=" w-11/12 h-11/12 float-right">
      <h5 class="font-bold">{{user.username}}</h5>
      <h4 class="font-extrabold"><a href="/post/{{id}}" class="dashboard-title">{{title}}</a></h4>
      {{/if}}
      <p>{{content}}</p>
      <a href="/dashboard/edit/{{id}}" class="dashboard-edit">Edit</a>
      </div>
  </div>
</div> --}}