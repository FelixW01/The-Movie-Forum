    <h1 class="text-center mt-4 text-5xl">Upcoming Movies</h1>

    <div class="grid grid-cols-3 my-0 sm:grid-cols-3 md:grid-cols-3 lg:grid-cols-3 xl:grid-cols-3 gap-x-5 gap-y-0 px-5 py-5 mx-auto w-12/12 h-5/6">

        <div class="flex items-center justify-center h-5/6 rounded-lg border-2 border-black">
        {{> forum-card movie}}
        </div>
        <div class="flex items-center justify-center h-5/6 rounded-lg border-2 border-black">
        {{> forum-card movie}}
        </div>
        <div class="flex items-center justify-center h-5/6 rounded-lg border-2 border-black">
        {{> forum-card movie}}
        </div>
        <div class="flex items-center justify-center h-5/6 rounded-lg border-2 border-black">
        {{> forum-card movie}}
        </div>
        <div class="flex items-center justify-center h-5/6 rounded-lg border-2 border-black">
        {{> forum-card movie}}
        </div>
        <div class="flex items-center justify-center h-5/6 rounded-lg border-2 border-black">
        {{> forum-card movie}}
        </div>
    </div>


{{!-- <div>

    {{#each movies as |movie|}}
    {{> forum-card movie}}
    {{/each}}

    <h1>hi</h1>
</div> --}}